@page "/venues"
@inject AppDbContext Db

<PageTitle>Venues – Bokningssystem</PageTitle>

<div class="page-header">
    <div>
        <h1 class="page-title">Venues</h1>
        <p class="page-subtitle">Arenor och lokaler för alla typer av evenemang</p>
    </div>
</div>

@if (venues is null)
{
    <div class="loading-spinner">
        <div class="spinner-ring"></div>
        <p>Hämtar venues...</p>
    </div>
}
else if (!venues.Any())
{
    <div class="empty-state">
        <div class="empty-icon">🏟️</div>
        <h3>Inga venues hittades</h3>
        <p>Inga venues registrerade ännu.</p>
    </div>
}
else
{
    <div class="venues-grid">
        @foreach (var v in venues)
        {
            <VenueCard Venue="@v" />
        }
    </div>
}

@code {
    private List<Models.Venue>? venues;

    protected override async Task OnInitializedAsync()
    {
        venues = await Db.Venues
            .Include(v => v.Events)
            .OrderBy(v => v.Name)
            .ToListAsync();
    }
}
